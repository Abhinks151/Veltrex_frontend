export type Tenant = {
  id: string;
  name: string;
  isBlocked: boolean;
  createdAt: string;
  updatedAt: string;
};

export type User = {
  id: string;
  name: string;
  email: string;
  isBlocked: boolean;
  createdAt: string;
  updatedAt: string;
};